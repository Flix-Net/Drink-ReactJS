import React from 'react';
import styled from "styled-components";

const InputBody = styled.select`
  width: 320px;
  height: 50px;
`

const Input = (props) => {
    return (
        <InputBody>

        </InputBody>
    );
};

export default Input;